services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - 8080:8080

    mem_limit: 256m
    cpus: "0.5"
    pids_limit: 64
  
    command: ["/app/api", "server"]

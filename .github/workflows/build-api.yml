name: Build api

on:
  pull_request:
    paths:
      - "apps/api/**"
      - ".github/workflows/build-api.yml"
      - ".github/actions/go-build/**"
  push:
    branches: [main]
    paths:
      - "apps/api/**"
      - ".github/workflows/build-api.yml"
      - ".github/actions/go-build/**"

permissions:
  contents: read

concurrency:
  group: build-api-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Vet and test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Build api
        uses: ./.github/actions/go-build
        with:
          module-dir: apps/api
          version-file: cmd/api/version.txt

      - name: Verify modules
        working-directory: apps/api
        run: go mod verify

      - name: Vet
        working-directory: apps/api
        run: go vet ./...

      - name: Test
        working-directory: apps/api
        run: go test -race -count=1 ./...
